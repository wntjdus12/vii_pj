#!/bin/bash

# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# nvm use node

# cd v_project

# git pull;

# SRC=/work/v_project/node_server
# DEST=/$HOME/deploy

# rm -rf $DEST
# mkdir -p $DEST
# cp -rf $SRC $DEST

# cd $DEST/node_server

# source ~/.bashrc

# npm install

# pm2 restart all